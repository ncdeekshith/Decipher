const menu=document.querySelector('.menu');const nav=document.querySelector('#navigation');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));nav.classList.toggle('open',open)});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&nav?.classList.contains('open')){nav.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.focus()}});
const form=document.querySelector('#consultation');if(form){const chosen=new URLSearchParams(location.search).get('service');if(['static','dynamic','enterprise'].includes(chosen))form.elements.service.value=chosen;form.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;const data=new FormData(form);const service=form.elements.service.selectedOptions[0].text;const body=`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nCompany: ${data.get('company')||'Not provided'}\nService: ${service}\n\nProject brief:\n${data.get('brief')}\n\nI agree to be contacted about this enquiry.`;location.href=`mailto:hello@decipherglobal.co?subject=${encodeURIComponent('Project enquiry — '+service)}&body=${encodeURIComponent(body)}`;document.querySelector('#form-status').textContent='Your enquiry is ready in your email app. It has not been sent by this website. If your email app did not open, email your brief to hello@decipherglobal.co.'})}
